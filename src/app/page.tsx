import { Header } from "@/components/common/header";
import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
